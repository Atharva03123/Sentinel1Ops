# SentinelOps – Python Dependencies
# Install with: pip install -r requirements.txt

psutil>=5.9.0          # System metrics (CPU, RAM, Disk)
psycopg2-binary>=2.9.6 # PostgreSQL adapter (binary = no build tools needed)
pandas>=2.0.0          # DataFrame processing & feature engineering
numpy>=1.24.0          # Numerical computation & regression
rich>=13.4.0           # Beautiful terminal UI (tables, panels, live)
